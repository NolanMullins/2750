#include <stdio.h>
#include <stdlib.h>

//worst case
class A
{
	int c;
	void fn(int a, int b)
	{
		c = a+b;
	}

	void fn(int a, char c)
	{
		c = a;
	}

	class A* gen()
	{
		class A* b = malloc(sizeof(class A));
		return b;
	}
};

int add (int a, int b)
{
	
}

/*this is a test
 * 
 */
int main()
{
	class A a;
	class A* b = malloc(sizeof(class A));
	int i = 0, j = 0;
	b->fn(&i,j);
	char myC;
	a.fn();
	a.fn(a,myC);
	return 0;
}
